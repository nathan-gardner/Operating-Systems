
/**
 * @brief Print a null-terminated string starting at (row, col) and ending with
 * the null terminated string.
 *
 * @param string
 * @param row
 * @param col
 */
void k_printstr(char *string, int row, int col); // written in assembly